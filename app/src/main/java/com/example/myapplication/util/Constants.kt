package com.example.myapplication.util

/**
 * Application-wide constants
 */
object Constants {
    /**
     * Offset used to create unique IDs for Song items in mixed lists
     * to avoid conflicts with Word IDs
     */
    const val SONG_ID_OFFSET = 10000
}
